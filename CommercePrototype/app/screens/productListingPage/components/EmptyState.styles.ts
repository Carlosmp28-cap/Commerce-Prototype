import { StyleSheet } from "react-native";

/**
 * Styles for the EmptyState component
 * Contains reusable style definitions for empty state UI elements
 */
export const styles = StyleSheet.create({
  suggestionText: {
    marginTop: 8,
    fontSize: 14,
    opacity: 0.7,
  },
});

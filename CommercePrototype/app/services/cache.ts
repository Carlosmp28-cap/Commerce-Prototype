// Cache layer: in-memory + persistence (AsyncStorage or MMKV).
// TODO: implement simple cache API: get(key), set(key, value, ttl?), invalidate(key).
// This module will be used by PLP/PDP data fetching (cache-first strategy).

export {}; // placeholder

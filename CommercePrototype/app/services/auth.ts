// Auth service: handle token storage, refresh logic and sign-in/out flows.
// TODO: implement functions: signIn(credentials), signOut(), getAccessToken(), refreshToken().
// Should integrate with storage.ts for persistence and with api.ts interceptors.

export {}; // placeholder

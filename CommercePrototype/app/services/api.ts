// API client wrapper (axios recommended)
// TODO: install axios and create instance with baseURL, interceptors for auth and error handling.
// Provide helper functions for GET/POST and support mock adapter in development.

// Example shape (pseudocode):
// import axios from 'axios'
// const api = axios.create({ baseURL: process.env.API_URL })
// api.interceptors.request.use(async (config) => { /* attach token */ })
// export default api

export {}; // placeholder to avoid TS errors until implemented

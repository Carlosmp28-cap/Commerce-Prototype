# Example environment variables for local development
# Replace <HOST_LAN_IP> with your machine's LAN IP (e.g. X.X.X.X)
EXPO_PUBLIC_API_URL=http://<HOST_LAN_IP>:5035

# Optionally point to ngrok or another tunnel during remote testing
# EXPO_PUBLIC_API_URL=https://abcd1234.ngrok.io
